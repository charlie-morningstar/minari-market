# 중고거래 플랫폼 실행에 필요한 파이썬 패키지 목록
# (버전을 고정해 두어야 채점 환경에서도 동일하게 재현된다)

Flask==3.0.3               # 웹 프레임워크
Flask-SocketIO==5.3.6      # 실시간 채팅(WebSocket)
simple-websocket==1.0.0    # SocketIO threading 모드용 웹소켓 백엔드
Flask-WTF==1.2.1           # CSRF 토큰 검증
Flask-Limiter==3.5.1       # 요청 속도 제한(무차별 대입/스팸 방지)
bcrypt==4.1.3              # 비밀번호 해시(느린 해시 + salt)
python-dotenv==1.0.1       # .env 로 비밀 설정값 관리
Pillow==10.3.0             # 업로드 이미지 실제 형식 검증
